{
  "name": "grass-backend",
  "version": "1.0.0",
  "description": "Grass - Touch Grass Backend API",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "npx prisma generate && npx prisma migrate deploy && node server.js",
    "dev": "node --watch server.js",
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate dev",
    "prisma:deploy": "npx prisma migrate deploy"
  },
  "keywords": ["grass", "productivity", "solana"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "@prisma/client": "^5.7.1",
    "ethers": "^6.9.0",
    "openai": "^4.20.1"
  },
  "devDependencies": {
    "prisma": "^5.7.1"
  }
}

